\frametitle{Решение}
    \begin{cpp}
int l = 0, r = 1e9;
while (l + 1 < r) {
    int m = (l + r) / 2;
    int remain = k - 1;
    int prev = st[0];
    for (int i = 1; i < n; i++) {
        if (st[i] - prev >= m) {
            prev = st[i];
            remain--;
        }
    }
    if (remain > 0) {
        r = m;
    } else l = m;
}
return l;
    \end{cpp}
